# The example project's dedicated environment (.venv), provisioned on first
# open. Everything the shipped notebook, analysis script and figure source
# import, so "Run" and the notebook kernel work without a setup step.
ipykernel
numpy
matplotlib
# plotly renders through the notebook mime bundle, which it only emits when
# nbformat is importable — without it a figure raises instead of drawing.
plotly
nbformat
