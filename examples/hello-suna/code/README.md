# code/

Reusable model code, imported by `analysis/` scripts and by figure sources.
Nothing here reads `data/` or writes `results/` — that is the pipeline's job,
and keeping the two apart is what makes the model testable on its own.
