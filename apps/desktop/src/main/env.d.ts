/**
 * electron-vite's ambient types for the main process — notably the `?asset`
 * import suffix, which copies a file into the build output and resolves to
 * its runtime path (used for the dock icon in index.ts).
 */
/// <reference types="electron-vite/node" />
